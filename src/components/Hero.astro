<section class="hero-section" aria-labelledby="hero-title">
  <div class="hero-bg"></div>

  <div class="hero-shell">
    <div class="hero-copy">
      <p class="hero-kicker">Nostr hudební mapa</p>
      <h1 id="hero-title" class="hero-title">
        <span>KAM</span>
        <span>NA</span>
        <span>RAVE</span>
      </h1>
    </div>

    <nav class="hero-nav" aria-label="Hlavní navigace">
      <a href="/akce">AKCE</a>
      <a href="/mapa">MAPA</a>
      <a href="/submit">PŘIDAT</a>
      <a href="#faq">FAQ</a>
    </nav>

    <div class="hero-meta" aria-label="Žánry">
      <span>TECHNO</span>
      <span>DNB</span>
      <span>FREETEKNO</span>
    </div>
  </div>

  <img src="/bunkr.png" class="bunkr" alt="" loading="eager" decoding="async" />
</section>

<style>
  .hero-section {
    position: relative;
    width: 100%;
    min-height: 100svh;
    overflow: clip;
    box-sizing: border-box;
    padding: 28px;
    display: grid;
    align-items: stretch;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    opacity: 0.055;
    background: url("/bg.jpeg") center / cover;
    z-index: 0;
  }

  .hero-shell {
    position: relative;
    z-index: 2;
    min-height: calc(100svh - 56px);
    border: 3px solid #000;
    overflow: hidden;
    padding: 22px;
    box-sizing: border-box;
    background: rgba(204, 0, 0, 0.18);
  }

  .hero-copy {
    position: absolute;
    inset: 22px;
    min-width: 0;
    pointer-events: none;
  }

  .hero-kicker {
    width: fit-content;
    border: 2px solid #000;
    background: #fff6df;
    color: #000;
    padding: 8px 10px;
    font-size: 0.82rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.12em;
  }

  .hero-title {
    position: absolute;
    inset: 48px 0 0;
    font-size: 12rem;
    line-height: 0.74;
    width: 100%;
  }

  .hero-title span {
    display: block;
    position: absolute;
    overflow-wrap: anywhere;
  }

  .hero-title span:nth-child(1) {
    top: 0;
    left: 0;
  }

  .hero-title span:nth-child(2) {
    top: 15svh;
    left: min(58%, 980px);
    transform: rotate(-2deg);
  }

  .hero-title span:nth-child(3) {
    top: 39svh;
    left: 24%;
    color: #fff6df;
    -webkit-text-stroke: 2px #000;
    text-shadow: 10px 10px 0 #000;
    transform: rotate(1deg);
  }

  .hero-nav {
    position: absolute;
    left: 22px;
    bottom: 22px;
    display: flex;
    align-items: end;
    flex-wrap: wrap;
    gap: 10px;
  }

  .hero-nav a {
    border: 3px solid #000;
    background: #fff6df;
    color: #000;
    padding: 12px 14px;
    text-decoration: none;
    font-size: 1.55rem;
    font-weight: 900;
    line-height: 1;
  }

  .hero-nav a:first-child {
    background: #000;
    color: #c00;
  }

  .hero-nav a:hover {
    background: #c00;
    color: #000;
  }

  .hero-meta {
    position: absolute;
    right: 22px;
    bottom: 22px;
    display: grid;
    gap: 10px;
    justify-items: end;
  }

  .hero-meta span {
    border: 3px solid #000;
    background: rgba(255, 246, 223, 0.84);
    padding: 12px 10px;
    font-family: var(--font-display);
    font-size: 2.2rem;
    line-height: 0.9;
    writing-mode: vertical-rl;
    text-orientation: mixed;
    justify-self: end;
  }

  .bunkr {
    position: absolute;
    z-index: 1;
    right: -36px;
    top: 28px;
    width: 360px;
    opacity: 0.72;
    pointer-events: none;
  }

  @media (max-width: 1180px) {
    .hero-shell {
      min-height: calc(100svh - 56px);
    }

    .hero-title {
      font-size: 9rem;
    }

    .hero-title span:nth-child(2) {
      left: min(50%, 500px);
    }

    .hero-meta span {
      font-size: 1.55rem;
    }

    .bunkr {
      width: 240px;
    }
  }

  @media (max-width: 768px) {
    .hero-section {
      min-height: 100svh;
      padding: 10px;
    }

    .hero-shell {
      min-height: calc(100svh - 20px);
      display: grid;
      grid-template-rows: auto 1fr auto auto;
      gap: 12px;
      padding: 14px;
      border-width: 2px;
      overflow: hidden;
    }

    .hero-copy {
      position: relative;
      inset: auto;
      display: grid;
      gap: 12px;
    }

    .hero-kicker {
      font-size: 0.7rem;
      padding: 6px 8px;
    }

    .hero-title {
      position: relative;
      inset: auto;
      display: grid;
      gap: 0;
      font-size: 5.4rem;
      line-height: 0.82;
    }

    .hero-title span {
      position: static;
    }

    .hero-title span:nth-child(2),
    .hero-title span:nth-child(3) {
      padding-left: 0;
      transform: none;
    }

    .hero-title span:nth-child(3) {
      text-shadow: 5px 5px 0 #000;
    }

    .hero-nav {
      position: relative;
      left: auto;
      bottom: auto;
      align-self: end;
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 6px;
    }

    .hero-nav a {
      padding: 10px 8px;
      font-size: 0.9rem;
      text-align: center;
      border-width: 2px;
    }

    .hero-meta {
      position: relative;
      right: auto;
      bottom: auto;
      display: flex;
      align-content: initial;
      align-items: center;
      flex-wrap: wrap;
      gap: 6px;
    }

    .hero-meta span {
      writing-mode: horizontal-tb;
      border-width: 2px;
      padding: 6px 8px;
      font-size: 0.92rem;
      font-family: var(--font-body);
      font-weight: 900;
    }

    .bunkr {
      width: 150px;
      right: -18px;
      top: 18px;
      opacity: 0.5;
    }
  }
</style>
